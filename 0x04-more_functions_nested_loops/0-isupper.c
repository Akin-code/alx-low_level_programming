#include "main.h"
/**
 * _isupper - return 1 if uppercase return if otherwise
 * @c: accepts integer value
 * Return: 0
 */
int _isupper(int c)
{
	if c >= 65 && c <= 90
	{
		return (1);
	}
	else
		return (0);
}
